[project]
name = "nequi-ds-compliance-risk"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pandas>=2.0", "numpy>=1.24"]

[tool.pytest.ini_options]
testpaths = ["tests", "risk_matrix/tests"]
addopts = "-q"
